/**
 * Check window visual perfection through Applitools Eyes service
 * @param  {Type}     windowName     The name of the window
 */
export default async (windowName) => {
    //await eyes.checkWindow(windowName);
};
